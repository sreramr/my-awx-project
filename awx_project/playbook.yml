# awx_project/playbook.yml
- hosts: localhost
  tasks:
    - name: Ensure the NTP service is enabled and running
      ansible.builtin.service:
        name: ntp
        enabled: yes
        state: started
